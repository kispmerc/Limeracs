# Limeracs

Core scripting framework for Minecraft Bedrock addons.

Modular architecture, lifecycle management, module dependencies, event system, scheduler, timers, storage, cache, schema validation, registry, player API, entity API, world API, block API, item API, inventory API, scoreboard, commands, UI, compatibility helpers, utilities, error handling, and debugging.
